# Personal Goal-based todos

To get better grip at prioritizing your todo's in life, assign them to your goals.

## Development

-   Set environment variables. See `.env.local.example` for that.
-   See `scripts` in `package.json`.
